{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "javac -d build -sourcepath src/ src/com/linhusp/twentyfortyeight/Start.java && cd build && java com.linhusp.twentyfortyeight.Start",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}
